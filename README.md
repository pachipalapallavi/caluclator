# Calculator Web App

A simple, responsive calculator built using **HTML**, **CSS**, and **JavaScript**.

## ✨ Features
- Performs basic arithmetic operations (+, −, ×, ÷)
- Responsive and modern UI
- Error handling for invalid expressions
- Easy-to-understand code structure

## 🛠️ Tech Stack
- HTML5 for structure
- CSS3 for styling
- Vanilla JavaScript for functionality

## 💡 How It Works
1. Each button click appends its value to a string (`currentInput`).
2. When `=` is clicked, the `eval()` function computes the result.
3. `C` clears the screen.

## 📂 Folder Structure
